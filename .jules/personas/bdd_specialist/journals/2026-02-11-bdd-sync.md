# BDD Synchronization Journal - 2026-02-11

## Status: Synchronizing BDD Features with Simplified Architecture

### Current State Assessment
- The project has transitioned from a complex `dlt`-based architecture to a simpler `httpx` + `DuckDB` implementation in `src/baliza/cli_simple.py` and `src/baliza/extractor.py`.
- There is a significant drift between the documented BDD features and the actual implementation.
- `backfill.feature` is missing entirely.
- `resilience.feature` has scenarios but no implemented steps.
- `state_management.feature` is using mocks/mappings instead of the actual state implementation.
- The "Tier" system documented in `docs/alignment/feature-hierarchy.md` is not yet fully reflected in the code or BDD tags.

### Objectives for this Sync
1. **Create `backfill.feature`**: Document the expected behavior for historical monthly backfills.
2. **Tag Features by Tier**: Align all BDD scenarios with the 4-tier hierarchy (@tier0 to @tier3).
3. **Implement Missing Core Logic**: Add `_start_run` and `_finish_run` to `PNCPExtractor` to support run history.
4. **Implement Missing CLI Commands**: Add a basic `backfill` command to `cli_simple.py`.
5. **Implement/Fix Step Definitions**: Ensure `test_resilience.py`, `test_backfill.py`, and `test_state_management.py` are functional and accurate.

### Progress Log
- [2026-02-11] Started BDD synchronization. Created journal directory and this initial entry.
- [2026-02-11] Identified missing `_start_run` and `_finish_run` in `extractor.py` as a blocker for history tests.
- [2026-02-11] Noted that `backfill` is documented in `ROADMAP.md` but missing from `cli_simple.py`.
