## 2026-01-14 - Enhancing CLI Readability with Rich
**Learning:** Adding icons to status tables and formatting large numbers with commas significantly improves scannability in CLI tools. Clickable links in success messages reduce friction.
**Action:** Always check if `rich` is available in CLI projects and leverage it for visual cues (icons) and interactive elements (links) instead of plain text.
